[Unit]
Description=Monitor 'test' process and send heartbeat to monitoring API
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/monitor-test.sh
User=root
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
